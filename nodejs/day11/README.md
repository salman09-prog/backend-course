# 📚 Bookstore API — Express + MVC Architecture

A simple Bookstore API built using **Express.js**, refactored into a clean
**MVC architecture** with modular routing, controllers, services, and utilities.
Data is stored in-memory (for learning purposes only).

---

## 🚀 Features

### 📘 Books CRUD Operations
- **GET /books** — List all books  
- **GET /books/:id** — Fetch book by ID  
- **POST /books** — Create a new book  
- **PUT /books/:id** — Update a book  
- **DELETE /books/:id** — Delete a book  

### 🧰 Middleware Used
- `express.json()` for parsing JSON bodies  
- Custom validation inside controllers  

---


